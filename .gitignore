# OS junk
Thumbs.db
desktop.ini
.DS_Store
ehthumbs.db

# Version marker written by deploy scripts (lives in user's Mods folder)
.mod-version

# Deploy scripts excluded from mod deployment (filtered in script itself)
# These are kept IN the repo so they can be fetched via one-liner commands

*settings.local.json